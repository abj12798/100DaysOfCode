#include <iostream>
#include <map>
#include <string>

using namespace std;

void valid_string(string out, int *arr, int n, int i, map<int, string> m, vector<string> &r)
{

    //base-case
    if (i == n)
    {
        r.push_back(out);
        return;
    }

    if (i < n)
    {
        out =
    }
}